{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:laravel": "vite build && npm run fix-laravel",
    "build:clean": "npm run clean-react && npm run build",
    "clean-react": "cd ../backend/public && (if exist assets rmdir /s /q assets) && (if exist index.html del index.html)",
    "fix-laravel": "cd ../backend/public && (if not exist index.php copy ..\\public\\index.php .) && echo ✓ Laravel fayllari saqlandi",
    "preview": "vite preview"
  }
  ,
  "dependencies": {
    "bootstrap": "^5.3.8",
    "lucide-react": "^0.556.0",
    "react": "^18.3.1",
    "react-bootstrap": "^2.10.10",
    "react-dom": "^18.3.1",
    "react-icons": "^5.5.0",
    "react-router-dom": "^6.20.0",
    "recharts": "^2.10.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@types/recharts": "^1.8.0",
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}